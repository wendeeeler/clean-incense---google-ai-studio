export interface NavItem {
  label: string;
  path: string;
}

export interface Ingredient {
  name: string;
  botanicalName: string;
  profile: string;
  behavior: string;
  benefits: string;
  sourcing: string;
}

export interface FaqItem {
  question: string;
  answer: string;
}
